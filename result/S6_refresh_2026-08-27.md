# HBOD S6 Refresh — three-month cohort (2026-08-27)

## Dataset

- file: `data/S6_cohort_football_100.csv`
- span: 2026-03-27 → 2026-06-26 (79 training days, three months)
- 8,993 daily entries, 100 players
- schema: `Timestamp, Email, Player, Session timing, Session duration (min),
  Session intensity (CR-10), Fatigue, Stress, Muscle soreness, Sleep,
  Specific pain, Location, OSTRC Q1..Q4`
- OSTRC items arrive as response options (text), not numbers

## Core changes required by this dataset

1. `COLMAP` OSTRC keys now accept `ostrc q1` / `ostrc-q1` as well as `ostrc_q1`.
   The previous build matched only the underscore spelling, so all four OSTRC
   columns were silently dropped for this export.
2. New `OSTRC_LABELS` + `_ostrc_score()` map the option text to the Clarsen
   per-item scores 0/8/17/25. Numeric input still takes precedence; unrecognised
   text yields `None` rather than a silent zero.

Non-regression: S5 re-run reproduces `validation/S5_example_audit.csv` exactly —
45/45 compared fields, 0 mismatches, INCREASE 1 / DECREASE 4.

## Run

```bash
python core/hbd_agent.py --input data/S6_cohort_football_100.csv \
    --date 2026-06-26 --out output/S6_3mo_report.html \
    --log output/S6_3mo_audit.csv --mode daily
```

Reference day 2026-06-26; recommendation for 2026-06-27.

## Squad totals

- cohort: 100 athletes
- decided on the reference day: **97**
- no decision (no entry that day): Player 018, Player 033, Player 049

**MAINTAIN 48 · DECREASE 33 · INCREASE 16**

These are the counts after the health override, which is the tally the tool
actually issues. `apply_health_override()` escalates both the STOP and the REFER
tier to DECREASE, per the three-tier design in
`result/explicit_formulas_edge_cases_parity_results.md` §1.7: an athlete told to
reduce load and see medical staff has had their load decision decreased, and is
counted as such.

For transparency, the tally under weaker escalation rules:

| convention | MAINTAIN | DECREASE | INCREASE |
| ---------- | -------- | -------- | -------- |
| A — load/wellness only, no health escalation | 51 | 30 | 16 |
| B — escalate STOP only | 50 | 31 | 16 |
| **C — escalate STOP + REFER (adopted)** | **48** | **33** | **16** |

An earlier draft snapshot reported 50 / 31 / 16, which corresponds to convention
B. That figure predates this decision and should be updated to 48 / 33 / 16
wherever it appears. The three athletes that move are 031 (STOP), 039 and 051
(REFER), all of whom are MAINTAIN on load and wellness alone.

## Health module

Reference-day tiers, unique players:

- **STOP 1** — Player 031 (Knee, OSTRC severity 100, "could not participate")
- **REFER 2** — Player 039, Player 051 (Shoulder, severity 68, reduced participation)
- **HOLD 3** — Player 016, Player 020, Player 050 (Back, severity 16, mild)

Matches the target 1 stop / 2 referrals / 3 holds.

Pre-override load decisions of the flagged athletes: 031, 039, 051 = MAINTAIN;
016, 020, 050 = DECREASE (already decreasing on load/wellness grounds, so the
HOLD tier does not change their outcome).

## Web app

Served with `HBD_DATA=data/S6_cohort_football_100.csv`:

- `POST /api/run` → `{"ok": true, "athletes": 97}`
- `GET /api/squad` → date 2026-06-26, recommendFor 2026-06-27, n 97,
  counts INCREASE 16 / MAINTAIN 48 / DECREASE 33, healthFlags 6
- `GET /api/audit.csv` vs the CLI audit: 97 athletes, 873/873 compared fields
  identical, 0 mismatches

This is a **same-core consistency check, not independent parity**: `app/main.py`
imports `hbd_agent` and every endpoint delegates to it, so the web layer holds no
second implementation of the maths. It cannot disagree with the CLI by
construction, and should not be reported as web-versus-Python validation.

## Resolved: REFER escalation

REFER escalates to DECREASE. The health tier outranks load and wellness, and the
tool never advises holding or increasing load for an athlete with a substantial
health problem, so the escalation is kept and the squad tally reflects it. The
consequence is that the reported split is 48 / 33 / 16 rather than the earlier
50 / 31 / 16; the code is unchanged by this decision, only the reported numbers.

## Artifacts

- `output/S6_3mo_report.html`
- `output/S6_3mo_audit.csv`
- `output/S6_3mo_output.txt`
